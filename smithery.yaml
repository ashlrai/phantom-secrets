# Smithery configuration for phantom-secrets-mcp
# Stop AI coding agents from leaking your API keys.
# Value-blind MCP plus an authenticated local proxy with exact route-owned auth injection.
startCommand:
  type: stdio
  configSchema:
    type: object
    properties: {}
  commandFunction: |-
    (config) => ({
      command: 'npx',
      args: ['-y', 'phantom-secrets-mcp']
    })
