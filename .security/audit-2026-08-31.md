# Phantom security audit continuation — 2026-08-31

## Scope and authority

This report covers the dirty integration candidate at
`/Users/masonwyatt/.codex/worktrees/phantom-integration-20260830`, based on
`27f94e98fb83880a4fe74143bb45ec4bba27f710`. It is source and local-test
evidence, not a commit, release, attestation, deployment, applied migration,
provider activation, signature, notarization, or customer acceptance.

## Corrected findings

| ID | Prior severity | Correction |
|---|---:|---|
| C-01 | High | Stripe claim, historical mapping resolution, monotonic event ordering, billing mutation, and completion now execute in one service-role-only PostgreSQL transaction; errors roll back the claim for retry, and request parsing is bounded before signature validation. |
| C-02 | High | Every public JSON/text API route uses a bounded streaming reader with strict UTF-8, object-only JSON, declared-length validation, actual-byte enforcement, and 400/413 failures. |
| C-03 | High | `phantom init` no longer leaves a plaintext `.env.backup`; Claude setup removes legacy exact dotenv read allows, preserves deny rules, and doctor reports the unsafe legacy state. |
| C-04 | High | Positional `phantom add KEY VALUE`, plaintext export, argv backup passphrases, and noninteractive reveal bypasses fail closed before mutation. Trusted-terminal or bounded private-file/stdin paths remain. |
| C-05 | High | Encrypted backup/restore uses zeroizing buffers, a minimum 12-byte passphrase, bounded private passphrase files, bounded archives, symlink/file-swap checks, private create-new staging, sync, atomic no-overwrite publication, and nonzero partial-restore failures. |
| C-06 | High | Actionable RustSec paths were upgraded (`anyhow`, `h2`, `self_update`, `notify`, `indicatif`); cargo-deny enforces advisories, licenses, bans/duplicates, and sources with warnings denied. The sole ignored advisory is the documented no-fix, dev-only RSA test dependency. |
| C-07 | High | The tag workflow gates on Gitleaks, cargo-deny, the full locked Rust suite, Clippy/fmt, wrapper/installer tests, web audit/tests/build, version parity, and MCP schema smoke before packaging. |
| C-08 | High | Six release archives have exact-set verification, 100 MiB limits, checksum sidecars, `SHA256SUMS`, archive-specific SPDX 2.3 SBOMs from checksum-verified Syft binaries, and permission-isolated GitHub attestation requests. |
| C-09 | Medium | `phantom upgrade` selects the exact platform archive rather than substring-matching SBOM/checksum sidecars and verifies the selected archive against release `SHA256SUMS`. |
| C-10 | Medium | Linked Git worktrees no longer plan an invalid `.git/hooks` mutation outside the contained workspace boundary. |
| C-11 | Medium | Public README, npm, LLM, recovery, grant-status, threat-model, platform, and release documentation now distinguishes shipped behavior, design-era goals, unreleased workflow source, and external acceptance. |
| C-12 | High | Billing checkout now recovers and conditionally persists one exact Stripe customer, uses stable per-user idempotency, blocks existing database or Stripe subscriptions, reuses one open checkout session, and fails closed on mapping/session ambiguity. Five executable route tests cover persistence failure, concurrency/repetition, existing subscriptions, and multiple open sessions. |
| C-13 | High | Repository secret scanning/push protection, private vulnerability reporting, Dependabot security updates, immutable releases, and SHA-only Actions are enabled. The release workflow now verifies a complete 19-asset draft receipt before publication and re-verifies the published immutable receipt. |

## Open activation and acceptance risks

| ID | Severity | Required evidence |
|---|---:|---|
| R-01 | High | Production execution remains deny-all pending a coherent signed Locus authority artifact, peer-authenticated native transport, confinement backend, trusted receipt signer, and rollback anchor. |
| R-02 | High | Supabase migrations in this candidate are unapplied. Database/RLS negative tests and authenticated Stripe, billing, team-vault, and Vercel acceptance require separate authorization and live evidence. |
| R-03 | High | The release workflow has not run on an immutable candidate. No SBOM, provenance attestation, archive, release, signature, notarization, or Authenticode result exists for these changes. |
| R-04 | Medium | Exact-archive native acceptance remains unperformed across the six target matrix. Local macOS source tests do not establish Windows/Linux credential-store, proxy, MCP, installer, or recovery behavior. |
| R-05 | Medium | RustSec `RUSTSEC-2023-0071` has no fixed RSA release and is limited by policy rationale to a dev-only test path; keep it out of normal dependency graphs and remove the exception when upstream fixes it. |
| R-06 | Medium | Stable Rust lacks exact Windows file identity and portable directory-fsync parity used by the Unix backup writer; Windows uses stable metadata fingerprint checks and inherited ACLs pending native acceptance or a narrowly reviewed platform API dependency. |

## Local verification evidence

- `cargo-deny 0.20.2 --all-features --locked check --deny warnings advisories licenses bans sources`: passed all four checks after dependency remediation and exact duplicate documentation.
- `cargo test --workspace --all-targets --locked --no-fail-fast`, strict all-target workspace Clippy, formatting, and `git diff --check` passed. Three authenticated cloud tests and one OS-keychain round trip remain intentionally ignored/manual.
- The final web rerun passed 18 tests, TypeScript, npm audit with zero production findings, and the 25-route Next.js production build. The Stripe migration also passed isolated PostgreSQL 17 lifecycle/rollback and runtime privilege checks; no Supabase state was changed.
- Both npm wrappers and package dry-runs passed. Direct installers passed eight offline tests; native PowerShell parsing was skipped because PowerShell is unavailable on this macOS host.
- Release artifact verifier tests include missing/extra/duplicate/symlink/malformed-SBOM and sparse oversize-archive failures. Publication/release-receipt harnesses passed 9 tests; the real package graph produced all 12 crates after formatting, strict Clippy, and the complete locked workspace suite passed. Workflow YAML, exact action pins, 0.7.0 parity across 8 pre-release surfaces/12 crates, and npm MCP tests passed. Homebrew remains on the last verified release until new archive checksums exist.
- Semgrep and Gitleaks completed with zero findings over a fresh 5.41 MB tracked-plus-untracked source mirror. A first broad Gitleaks directory scan was discarded because it included generated `.next` and `target` output; Semgrep's sole initial finding, a global publisher `IFS` assignment, was removed before the final scan.
- The atomic Stripe RPC was applied only to an isolated PostgreSQL 17 cluster and passed applied, stale, delete, new-checkout, superseded, duplicate, missing-mapping rollback, ownership, search-path, and grant checks.

## Decision

This is a materially hardened and locally verified candidate. The user has
authorized production publication, and the repository control-plane protections
listed above are active. The candidate is not yet a merged commit, immutable
release, deployed web application, applied migration, provider activation, or
authenticated customer acceptance; those gates remain explicit and are recorded
separately.
