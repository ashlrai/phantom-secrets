# Phantom security audit — 2026-08-30

## Scope and identity

This is a local working-tree audit of the clean reconciliation candidate. It is
not an immutable release, deployment, provider, database, or customer
attestation.

| Field | Value |
|---|---|
| Repository | `ashlrai/phantom-secrets` |
| Worktree | `/Users/masonwyatt/.codex/worktrees/phantom-integration-20260830` |
| Branch | `codex/phantom-integration-20260830` |
| Base `HEAD` | `27f94e98fb83880a4fe74143bb45ec4bba27f710` |
| State | Dirty integration candidate; no content SHA exists for these changes |
| Reviewed | Rust workspace, CLI/MCP/proxy/vault/kernel boundaries, npm wrappers, direct installers, release/CI workflows, Next.js application, Supabase migration source, GitHub metadata, and canonical documentation |
| Excluded external state | GitHub settings, published artifacts, package registries, provider applications, live billing/OAuth, deployed web state, and applied Supabase schema |

## Executive result

No unresolved critical or high-severity source finding was identified in the
active local product paths by this review. The candidate now fails closed at
the previously dangerous plaintext, revocation, endpoint-override, response
encoding, and inactive execution-authority boundaries. Static analysis and the
source-only secret scan are clean.

This is not a production-readiness claim. The externally governed and
architectural blockers below remain open, and the candidate is still an
uncommitted working tree.

## Corrected findings

| ID | Severity before correction | Finding | Correction and evidence |
|---|---:|---|---|
| S-01 | Critical | Shipped provider issuance honored runtime endpoint/mock overrides, allowing an agent-controlled environment to redirect credential exchange. | Production endpoints now come from the closed provider map; injection is test-only. Five shipped-endpoint regression tests and 63 focused issuance tests passed. |
| S-02 | High | Proxy response scrubbing did not consistently forward the scrubbed header/body bytes, and encoded upstream responses could bypass safe inspection. | The proxy forces upstream identity, rejects every non-identity encoding before forwarding, and scrubs response headers plus buffered/streaming bodies. The converged proxy suite passed 223 tests. |
| S-03 | High | CLI plaintext/revocation paths allowed noninteractive reveal acknowledgement, plaintext JSON export, stdout clipboard fallback, and non-atomic team offboarding behavior. | Reveal requires attached terminals and exact typed confirmation; legacy `--yes` is rejected; clipboard failure returns an error; plaintext JSON export is disabled; team and provider revoke fail before auth, network, vault, config, or audit mutation. Five CLI hardening and two provider-revoke integration tests passed. |
| S-04 | High | npm/direct installation paths lacked one coherent bounded, transactional artifact contract across every platform target. | Both npm wrappers and direct installers now enforce private staging/cache state, HTTPS and redirect bounds, strict checksums, exact archive members, binary version identity, ownership-aware locking, rollback, and the same closed six-target map. Wrapper, package, installer, and release-artifact suites passed. |
| S-05 | High | Web billing/OAuth/team-vault paths and pending RLS source had authentication, access-control, and dependency-risk gaps. | Browser billing auth is separated from CLI device tokens; Vercel routes fail closed; team wrapped-key input is validated; response headers and pending RLS are hardened. Production and full npm audits report zero advisories, eight web tests pass, TypeScript passes, and the 25-route production build succeeds. |
| S-06 | Medium | A published GitHub Actions example used the mutable `actions/checkout@v4` tag. | The example now uses the same reviewed 40-character action commit as CI. Repository-wide Semgrep returned zero findings after correction. |

## Open risks and activation blockers

| ID | Severity | Status and required next evidence |
|---|---:|---|
| R-01 | High | Governed production execution remains deliberately unavailable. There is no coherent signed Locus authority artifact, peer-authenticated native transport, production confinement backend, trusted receipt signer, or host-protected monotonic rollback anchor. Deny-all constructors contain the risk; activation requires a separately reviewed protocol and native acceptance. |
| R-02 | High | The six release targets lack exact-archive native acceptance, per-artifact SBOMs, provenance attestations, independent signatures, macOS signing/notarization, and Windows Authenticode. Checksums and source builds do not establish publisher identity or native trust. |
| R-03 | High | Supabase hardening is source-only and unapplied. Database-backed negative RLS tests, browser billing, Vercel OAuth, provider consent/renewal, and deployed customer flows require separately authorized external acceptance. |
| R-04 | Medium | Encrypted export still accepts its passphrase through argv and writes the final backup through the legacy file path. A follow-up should add hidden trusted-terminal/passphrase-file input and a private create-new atomic writer without silently breaking recovery automation. |
| R-05 | Medium | `cargo-audit`/`cargo-deny` and Cargo Vet are not installed or enforced locally. Rust dependency advisory and reviewed-supply-chain evidence is therefore unavailable even though the locked build, tests, and Clippy pass. |
| R-06 | Medium | Native PowerShell parsing and the complete six-platform installer/credential-store/proxy/MCP acceptance matrix were not available on this macOS host. CI defines Windows parsing/build coverage, but an actual CI result and immutable artifacts are still required. |

## Verification evidence

- `cargo test --workspace --all-targets --locked --no-fail-fast`: passed; three authenticated cloud tests and one real OS-keychain roundtrip remained ignored/manual.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: passed.
- `cargo fmt --all -- --check` and `git diff --check`: passed.
- Primary npm wrapper: all platform, manifest, cache, replacement, archive, lock, and signal hardening tests passed; package dry-run passed.
- MCP npm wrapper: all six suites passed, including schema and exact release-artifact contracts; package dry-run passed.
- Direct installers: eight offline tests passed; native PowerShell parse test skipped because `pwsh` is unavailable locally.
- Web: `npm audit --omit=dev` reported zero vulnerabilities; 8/8 tests, TypeScript, and the Next.js 15.5.24 production build passed.
- Semgrep 1.155.0: 351 rules over 349 tracked and untracked source targets, zero findings. Two files over 1 MiB and reviewed ignore patterns were skipped.
- Gitleaks 8.30.0: reviewed tracked-plus-untracked source copy, approximately 5.26 MB, zero findings; temporary scan material was moved to Trash.
- Documentation: 42 Markdown files passed relative-path validation; GitHub YAML and public JSON/package metadata parsed; stale five-target, 25-tool, plaintext-reveal, and five-crate claims were removed.

## Release decision

Suitable as a locally verified reconciliation candidate for human review. Not
yet suitable for a trusted release or production execution activation. Before
promotion: create an exact reviewable commit, rerun CI on that SHA, produce and
verify the six immutable archives, close or explicitly accept R-01 through
R-06, and record publication/deployment/provider/database/customer evidence as
separate approvals and receipts.
