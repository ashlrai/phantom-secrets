{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.ashlrai/phantom-secrets-mcp",
  "title": "Phantom Secrets",
  "description": "Value-blind secret metadata and gated workflows for AI coding agents through Phantom's local proxy.",
  "websiteUrl": "https://github.com/ashlrai/phantom-secrets",
  "repository": {
    "url": "https://github.com/ashlrai/phantom-secrets",
    "source": "github",
    "subfolder": "crates/phantom-mcp"
  },
  "version": "0.7.6",
  "tools": [
    {
      "name": "phantom_add_secret",
      "description": "Deprecated unsafe plaintext secret entry. This tool refuses values because MCP arguments enter agent context. Use phantom_add_secret_interactive instead.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.\nFormat: \"<nonce_hex>:<approval_token_hex>\".",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. Defends against prompt-injected instructions\nin project content (READMEs, issue comments, dependency docs) silently\nmutating the vault.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the secret (e.g., OPENAI_API_KEY)",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "AddSecretParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_add_secret_interactive",
      "description": "Safely add a secret by name without passing its value through MCP. Requires confirm:true. The returned command prompts for the value directly in the user's terminal, outside agent context.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore starting an out-of-band terminal flow.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the secret to add (e.g., OPENAI_API_KEY)",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "AddSecretInteractiveParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_apply_expiry_policy",
      "description": "Scan the vault, apply read-only demotion to secrets whose TTL has expired, and (optionally) re-promote secrets that were demoted but have since been rotated. This blocks future vault retrieval and new `phantom exec`/foreground `phantom start` mapped-secret preflight; it does not recall values already injected or cached. Returns: { demoted: [{ name, expires_at, secs_overdue }], promoted: [{ name }], skipped_count, total_scanned }. Requires confirm:true because it writes vault metadata.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_apply_expiry_policy`.\n\nScans the vault, identifies secrets whose TTL has expired, sets\n`vault_mode = ReadOnly` on each, and returns a list of affected secrets.\nThis is the demotion step that blocks future vault retrieval and new\n`phantom exec`/foreground `phantom start` mapped-secret preflight. It does\nnot recall values already injected into or cached by running processes.\n\nRequires `confirm: true` because it writes metadata.",
        "properties": {
          "also_promote_rotated": {
            "default": true,
            "description": "When `true`, also promote secrets that were previously demoted but have\nsince been rotated (vault_mode ReadOnly → ReadWrite if `rotated_at` is\nmore recent than `expires_at`).  Default: `true`.",
            "type": "boolean"
          },
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Must be `true`.  The caller must obtain user consent before demoting\nsecrets to read-only mode. New retrieval and process preflight fail;\nalready injected or cached values are not recalled.",
            "type": "boolean"
          }
        },
        "title": "ApplyExpiryPolicyParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_alerts",
      "description": "Retrieve persisted leak-incident alert records from ~/.phantom/leak-alerts.jsonl. Each alert represents a high-confidence proxy response leak that was dispatched to configured backends (webhook/Slack/PagerDuty). Set 'backfill: true' to re-run correlation, persist new incidents/alerts, and dispatch configured notifications before returning the list; this requires confirm plus approval_token. Returns the most recent 'last' alerts (default 50) in chronological order, including: secret_name, location_label, confidence, event_count, alerted_at, backends_notified, and remediation advice. Never exposes secret values. Read-only when backfill=false. Safe for AI agents.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Parameters for `phantom_audit_alerts`.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "backfill": {
            "default": false,
            "description": "If true, re-run the leak correlation engine and emit pending alerts\nbefore returning the list. Default: false.",
            "type": "boolean"
          },
          "confirm": {
            "default": false,
            "description": "Required when `backfill=true` because correlation state and alert\nrecords may be persisted and notification backends may be called.",
            "type": "boolean"
          },
          "last": {
            "default": 50,
            "description": "Maximum number of recent alerts to return (default: 50).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "AuditAlertsParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_analytics",
      "description": "Export full audit analytics for compliance dashboards. Returns per-secret SecretAnalytics (access_count, daily_avg, max_daily, min_daily, anomaly_score) plus timestamped AccessRecords for the requested window. Supports JSON or CSV format. Use min_anomaly_score to filter to flagged secrets only. Never exposes secret values. Feed the output to Datadog, Grafana, CloudWatch, or any BI tool. window_days=0 returns all history; default is 30 days. format=\"csv\" produces RFC 4180 CSV; format=\"json\" produces structured JSON with 'analytics' (SecretAnalytics[]) and 'records' (AccessRecord[]) arrays.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_audit_analytics` — full analytics + access-record export.",
        "properties": {
          "format": {
            "default": "json",
            "description": "Output format: \"json\" (default) or \"csv\".",
            "type": "string"
          },
          "min_anomaly_score": {
            "default": null,
            "description": "Only include secrets whose anomaly score is at or above this threshold\n(range 0.0–1.0). Defaults to 0.0 (return all secrets).",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "window_days": {
            "default": 30,
            "description": "Time window in days (default: 30). 0 means all available history.",
            "format": "uint32",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "AuditAnalyticsParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_anomalies",
      "description": "Query the audit log for suspicious access patterns. Returns a findings array where each entry has: name (secret name), anomaly_type (spike | dormant | first_access), anomaly_score (0.0–1.0), access_count, last_access (ISO-8601), daily_avg, and context (human-readable explanation). Secret VALUES are never returned. Read-only; no confirm required. Anomaly types: 'spike' = single day >3x daily average; 'dormant' = access after >=7 consecutive quiet days. Use min_score to filter (default 0.4). Use period to limit the window.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "min_score": {
            "default": 0.4,
            "description": "Minimum anomaly score to include (0.0–1.0). Defaults to 0.4.",
            "format": "double",
            "type": "number"
          },
          "period": {
            "default": "30d",
            "description": "Time period to analyse: \"7d\", \"30d\", or \"all\". Defaults to \"30d\".",
            "type": "string"
          }
        },
        "title": "AuditAnomaliesParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_anomalies_realtime",
      "description": "Check whether a secret (or all secrets) has been accessed unusually based on windowed rate and quiet-period analysis of the current audit log. Unlike phantom_audit_anomalies (which uses multi-day daily-bucket statistics), this tool evaluates: (1) accesses within the last rolling hour vs max_accesses_per_hour threshold; (2) re-access after a quiet period longer than max_consecutive_quiet_days. Returns a findings array with fields: name, anomaly_score (0.0–1.0), alert (bool), reason (string), accesses_last_hour, max_quiet_gap_days. Secret VALUES are never returned. Read-only; no confirm required. Use threshold (default 0.5) to filter results. Pass name to check a single secret. Per-secret overrides from .phantom.toml [phantom.secrets.{name}.audit] are respected when max_accesses_per_hour / max_consecutive_quiet_days are omitted.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_audit_anomalies_realtime` — windowed, per-second\nrate-based anomaly check against the current audit log state.",
        "properties": {
          "max_accesses_per_hour": {
            "default": null,
            "description": "Override: max accesses allowed within any rolling 1-hour window before\nflagging a rate spike. Defaults to 100.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "max_consecutive_quiet_days": {
            "default": null,
            "description": "Override: number of consecutive quiet days after which a re-access\ntriggers a quiet-period alert. Defaults to 7.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "name": {
            "default": null,
            "description": "Filter to a single secret name. Omit to check all secrets.",
            "type": [
              "string",
              "null"
            ]
          },
          "threshold": {
            "default": 0.5,
            "description": "Minimum anomaly score (0.0–1.0) to include in results. Defaults to 0.5.",
            "format": "double",
            "type": "number"
          }
        },
        "title": "AuditAnomaliesRealtimeParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_export_report",
      "description": "Export audit log data or generate a structured compliance report. Two actions: 'export' — return raw audit rows (timestamp, datetime, operation, secret_name, pid, hostname, severity) filtered by date range, secret name, or operation type, in CSV or JSON format; 'report' — generate a full compliance report containing: (a) access-frequency heatmap per secret per calendar day, (b) leak-incident timeline with incident_id/secret_name/occurrences, (c) rotation-timing audit showing days since last vault.store per secret, (d) anomaly executive summary of high-score secrets. Use 'from'/'to' (YYYY-MM-DD) to scope the date range. Set 'save: true' to persist the report to ~/.phantom/reports/; saving requires confirm plus approval_token. Never exposes secret values. Read-only (save=false). Safe for AI agents.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Parameters for `phantom_audit_export_report`.",
        "properties": {
          "action": {
            "default": "report",
            "description": "Action: \"export\" to get raw rows as CSV/JSON, or \"report\" to get a\nfull compliance report. Default: \"report\".",
            "type": "string"
          },
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required when `save=true` because a report is persisted to disk.",
            "type": "boolean"
          },
          "format": {
            "default": "json",
            "description": "Output format: \"csv\" or \"json\". Default: \"json\".",
            "type": "string"
          },
          "from": {
            "default": null,
            "description": "Start date inclusive (YYYY-MM-DD). Omit for no lower bound.",
            "type": [
              "string",
              "null"
            ]
          },
          "operation": {
            "default": null,
            "description": "Filter rows to operations containing this string. Omit for all ops.",
            "type": [
              "string",
              "null"
            ]
          },
          "save": {
            "default": false,
            "description": "Save the compliance report to ~/.phantom/reports/ (report action only).",
            "type": "boolean"
          },
          "secret_name": {
            "default": null,
            "description": "Filter rows to this secret name only (exact match). Omit for all secrets.",
            "type": [
              "string",
              "null"
            ]
          },
          "to": {
            "default": null,
            "description": "End date inclusive (YYYY-MM-DD). Omit for no upper bound.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "title": "AuditExportReportParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_hotspot_alerts",
      "description": "Inspect per-secret access-velocity spike alerts without manually parsing the audit log. Returns alerts for secrets whose access count in the rolling 24-hour window either (a) exceeds 5× the 7-day mean baseline, or (b) includes a burst of ≥100 accesses within any 5-minute window. Each alert record contains: secret_name, current_velocity (24h count), baseline_velocity (7-day daily mean), alert_level (always 'high'), first_spike_ts (ISO-8601), ack_status ('unacked'|'acked'|'snoozed'), peak_5m_count, and trigger (human-readable description). Secret VALUES are never returned. Set ack=true to acknowledge all returned alerts (removes them from the active list). Set snooze_seconds>0 with ack=true to snooze instead of fully acknowledging. Pass include_acked=true to also return already-acknowledged or snoozed alerts. Use secret_name to filter to a specific secret. Read-only when ack=false; ack=true persists acknowledgement state and requires confirm plus approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Parameters for `phantom_audit_hotspot_alerts` — inspect and acknowledge\nper-secret access-velocity spike alerts.",
        "properties": {
          "ack": {
            "default": false,
            "description": "If true, acknowledge (clear) all returned unacked alerts.\nDefault: false (read-only inspection).",
            "type": "boolean"
          },
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required when `ack=true` because acknowledgement state is persisted.",
            "type": "boolean"
          },
          "include_acked": {
            "default": false,
            "description": "Include already-acknowledged or snoozed alerts in the response.\nDefault: false (only unacked alerts).",
            "type": "boolean"
          },
          "secret_name": {
            "default": null,
            "description": "If set, only return alerts for this specific secret name.",
            "type": [
              "string",
              "null"
            ]
          },
          "snooze_seconds": {
            "default": 0,
            "description": "If > 0 and `ack` is true, snooze the alert for this many seconds instead\nof fully acknowledging it. Default: 0 (full ack when `ack` is true).",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "AuditHotspotAlertsParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_incidents",
      "description": "Return active leak incidents derived from proxy.response_leak audit events. Incidents are correlated by (secret_name, location) within a 24-hour window. min_confidence (default 0.7) filters by confidence score: 0.5 = single leak event; 0.95 = same secret leaked >3 times within 1 hour. Each incident includes: incident_id, secret_name (never the value), location_label, first_seen_ts, last_seen_ts, event_count, confidence, remediation. Incidents whose secret was subsequently stored are omitted. Reads only the persisted incident store; it does not run correlation or write state.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_audit_incidents`.",
        "properties": {
          "min_confidence": {
            "default": 0.7,
            "description": "Only return incidents whose confidence score is at or above this value\n(range 0.0–1.0). Defaults to 0.7.",
            "format": "double",
            "type": "number"
          }
        },
        "title": "AuditIncidentsParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_recent",
      "description": "List the last N audit events from the HMAC-chained audit log. Returns structured JSONL — each line is a JSON object with fields: seq, ts (Unix epoch), op (operation name), name (secret name, if applicable), pid, process. Secret VALUES are never recorded in the audit log and will never appear here. Supports optional filtering by op prefix or exact secret name. Read-only; no confirm required. Requires PHANTOM_AUDIT=1 to have been set when operations were performed.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "n": {
            "default": 20,
            "description": "Maximum number of recent audit events to return (default: 20, max: 200).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          },
          "name_filter": {
            "default": null,
            "description": "Filter by secret name (exact match). Omit for all secrets.",
            "type": [
              "string",
              "null"
            ]
          },
          "op_filter": {
            "default": null,
            "description": "Filter by operation name prefix (e.g. \"vault.retrieve\", \"cloud\"). Omit for all ops.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "title": "AuditRecentParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_audit_stats",
      "description": "Aggregate access counts, last-access timestamps, daily averages, and anomaly scores from the HMAC-chained audit log. Returns JSON only — never exposes secret values. Anomaly detection flags: (1) any single day with >3× the daily average access count (score 0.6); (2) first access after ≥7 days of inactivity (score 0.5). Use period to limit the window (\"7d\", \"30d\", \"all\"). Use min_anomaly_score to filter to flagged secrets only (e.g. 0.5). Requires PHANTOM_AUDIT=1 to have been set when secrets were accessed.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "min_anomaly_score": {
            "default": null,
            "description": "Only include secrets whose anomaly score is at or above this threshold\n(range 0.0–1.0). Omit or set to 0.0 to return all secrets.",
            "format": "double",
            "type": [
              "number",
              "null"
            ]
          },
          "period": {
            "default": "all",
            "description": "Time period to analyse: \"7d\", \"30d\", or \"all\". Defaults to \"all\".",
            "type": "string"
          }
        },
        "title": "AuditStatsParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_capability",
      "description": "Return a value-free capability card for the small conversation-native facade: allowed local verbs, requestable verbs, place/seal state, expiry, and facade hard denials. This is not a policy oracle for the advanced compatibility catalog, whose mutating tools retain separate legacy confirm plus out-of-band local approval gates and are not Locus-sealed.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "phantom_check",
      "description": "Check for security issues. With runtime=true, scans current environment for phantom tokens without a proxy (leak detection). Otherwise, scans .env files for unprotected real secrets.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "runtime": {
            "default": false,
            "description": "Check if phantom tokens are in environment without proxy running",
            "type": "boolean"
          }
        },
        "title": "CheckParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_cloud_pull",
      "description": "Pull and decrypt a personal-vault snapshot from a separately commissioned Phantom Cloud deployment and entitled account; the public hosted service is not currently commissioned for authenticated use. DESTRUCTIVE — writes local vault entries; force=true declares overwrites but never bypasses approval. With force=false, existing entries are skipped; any partial result preserves the prior merge base, records a durable reconciliation requirement, and blocks cloud push until a fully reconciled pull. Requires `confirm: true` plus an out-of-band `approval_token`.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. A pull writes entries into the local vault;\nforce declares which reviewed entries may be overwritten.",
            "type": "boolean"
          },
          "force": {
            "default": false,
            "description": "Declare overwrite of existing local secrets (default: false). This does\nnot bypass confirmation or out-of-band approval. With false, skipped\nentries retain the prior merge base and block push until reconciliation.",
            "type": "boolean"
          }
        },
        "title": "CloudPullParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_cloud_push",
      "description": "Push a client-encrypted local vault to a separately commissioned Phantom Cloud deployment and entitled account; the public hosted service is not currently commissioned for authenticated use. The configured service receives ciphertext rather than decrypted vault values. Requires phantom login first. DESTRUCTIVE — overwrites the existing cloud copy; damage from a prompt-injected push propagates to every machine that later pulls. Requires `confirm: true`; the agent must ask the user for explicit consent before calling.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. A push overwrites the cloud copy of the\nproject's vault; damage from a prompt-injected push propagates to every\nmachine that later pulls.",
            "type": "boolean"
          }
        },
        "title": "CloudPushParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_cloud_status",
      "description": "Check authentication status, plan, and last sync version against a separately commissioned Phantom Cloud deployment; the public hosted service is not currently commissioned for authenticated use. This uses the stored cloud credential and makes a provider request; requires confirm plus approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Dual approval gate for tools whose only inputs are authority controls.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true before the provider or persistent-state effect.",
            "type": "boolean"
          }
        },
        "title": "ApprovalParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_compliance_status",
      "description": "Return the compliance state of the current Phantom project as a structured JSON badge. Checks: vault_accessible (vault can be read), audit_enabled (PHANTOM_AUDIT env var is set), precommit_installed (git pre-commit hook contains the current local-only phantom check), env_clean (no unprotected real secrets in .env), secrets_have_ttl (all vault secrets have a rotation policy set). Each check is true/false with an optional detail string. Overall 'compliant' is true only when all checks pass. Read-only.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "title": "ComplianceStatusParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_copy_secret",
      "description": "Copy a secret into a distinct initialized target through one exact-before target transaction. Refuses existing target vault, lifecycle-config, or managed-dotenv ownership; never overwrites. On success it creates both the target vault entry and managed-dotenv phantom mapping, with dotenv committed last. This is an exfiltration-capable cross-project write and requires confirm plus an out-of-band approval token before any source value retrieval.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. Copying writes secrets into another vault,\nwhich an attacker can use as an exfiltration primitive.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the secret to copy from the current project",
            "type": "string"
          },
          "rename": {
            "description": "Optional new name for the secret in the target project. Copy refuses\nif the target vault, lifecycle config, or managed dotenv already owns\nthis name; there is no overwrite mode.",
            "type": [
              "string",
              "null"
            ]
          },
          "target_dir": {
            "description": "Path to the target project directory (must be phantom-initialized).\n`..` segments are rejected to prevent prompt-injected target-dir\nobfuscation; pass the full destination path explicitly.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "target_dir"
        ],
        "title": "CopySecretParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_do",
      "description": "Conversation-native engineering action planning. phase=propose (default) accepts only Phantom's closed Cargo action schema and returns a value-free canonical digest, effect classification, local workspace fingerprint, and exact activation blockers without executing anything. phase=execute is reserved and always hard denied until verified Locus authority, rollback-resistant replay, trusted handles, OS confinement, and correlated evidence are active. There are no bearer or secret-value fields; bounded action selectors are never treated as authority and are never echoed.",
      "inputSchema": {
        "$defs": {
          "EngineeringAction": {
            "oneOf": [
              {
                "additionalProperties": false,
                "properties": {
                  "action": {
                    "const": "cargo_check",
                    "type": "string"
                  },
                  "cwd": {
                    "$ref": "#/$defs/RelativeCwd"
                  },
                  "package": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/PackageName"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  }
                },
                "required": [
                  "action",
                  "cwd"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "properties": {
                  "action": {
                    "const": "cargo_test",
                    "type": "string"
                  },
                  "cwd": {
                    "$ref": "#/$defs/RelativeCwd"
                  },
                  "filter": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/TestFilter"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  },
                  "package": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/PackageName"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  }
                },
                "required": [
                  "action",
                  "cwd"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "properties": {
                  "action": {
                    "const": "cargo_clippy",
                    "type": "string"
                  },
                  "cwd": {
                    "$ref": "#/$defs/RelativeCwd"
                  },
                  "package": {
                    "anyOf": [
                      {
                        "$ref": "#/$defs/PackageName"
                      },
                      {
                        "type": "null"
                      }
                    ]
                  }
                },
                "required": [
                  "action",
                  "cwd"
                ],
                "type": "object"
              },
              {
                "additionalProperties": false,
                "properties": {
                  "action": {
                    "const": "cargo_fmt_check",
                    "type": "string"
                  },
                  "cwd": {
                    "$ref": "#/$defs/RelativeCwd"
                  }
                },
                "required": [
                  "action",
                  "cwd"
                ],
                "type": "object"
              }
            ]
          },
          "EngineeringDoPhase": {
            "oneOf": [
              {
                "const": "propose",
                "description": "Canonicalize and report one exact closed action without executing it.",
                "type": "string"
              },
              {
                "const": "execute",
                "description": "Reserved for the future sealed runtime. Currently always denied.",
                "type": "string"
              }
            ]
          },
          "PackageName": {
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_.-]{0,127}$",
            "type": "string"
          },
          "RelativeCwd": {
            "maxLength": 512,
            "minLength": 1,
            "type": "string"
          },
          "TestFilter": {
            "maxLength": 128,
            "minLength": 1,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_.:-]{0,127}$",
            "type": "string"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "action": {
            "$ref": "#/$defs/EngineeringAction",
            "description": "One closed Cargo engineering action. Arbitrary shell commands are not representable."
          },
          "phase": {
            "$ref": "#/$defs/EngineeringDoPhase",
            "default": "propose",
            "description": "`propose` (default) or `execute` (currently hard denied)."
          }
        },
        "required": [
          "action"
        ],
        "title": "EngineeringDoParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_doctor",
      "description": "Run repository-local Phantom health checks: config validity, .env protection, .gitignore, .env.example, and pre-commit hook. This tool does not open or provision a vault. Set fix=true to apply reviewed repository-local repairs.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`. Only\nrequired when fix=true (read-only doctor runs do not need approval).",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required when fix=true because files may be created or modified.",
            "type": "boolean"
          },
          "fix": {
            "default": false,
            "description": "Auto-fix safe issues (install hooks, generate .env.example, etc.)",
            "type": "boolean"
          }
        },
        "title": "DoctorParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_env",
      "description": "Generate a .env.example file from .env. Secrets are replaced with descriptive placeholders; non-secret config values are preserved. Safe to commit to version control.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true because this writes an env example file.",
            "type": "boolean"
          },
          "output": {
            "default": ".env.example",
            "description": "Output file name (defaults to .env.example)",
            "type": "string"
          }
        },
        "title": "EnvParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_expiry_enforce",
      "description": "Scan .phantom.toml for expired secrets and return a structured list. Each entry has: name (string), expires_at (Unix u64), secs_overdue (u64), status (human-readable string). With fail_closed=true, secrets that have no expiry policy set are also included with status='no_expiry_policy'. Returns { expired: [...], ok_count, no_expiry_count, fail_closed, pass }. pass=true means no action is needed. Read-only; no confirm required. Secret VALUES are never returned.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_expiry_enforce`.",
        "properties": {
          "fail_closed": {
            "default": false,
            "description": "If true, also treat secrets with no expiry policy as violations.\nDefaults to false (permissive: only hard-expired secrets are reported).",
            "type": "boolean"
          }
        },
        "title": "PhantomExpiryEnforceParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_init",
      "description": "Initialize Phantom through one exact-before project transaction: read a safe project-local dotenv, compare-and-swap every vault entry, persist config, and rewrite the dotenv with phantom tokens last. Concurrent file or vault changes abort and transaction-owned writes are rolled back where verifiable. Requires confirm plus out-of-band approval.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.\nFormat: \"<nonce_hex>:<approval_token_hex>\".",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true because init stores secrets and rewrites .env.",
            "type": "boolean"
          },
          "env_path": {
            "default": ".env",
            "description": "Path to the .env file (defaults to .env in current directory)",
            "type": "string"
          }
        },
        "title": "InitParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_leak_incidents_realtime",
      "description": "Query active leak incidents for a real-time security dashboard. Returns incidents with confidence > 0.5 seen within the last 24 hours, sorted by confidence descending. Each entry contains: secret_name (never the value), location_label, confidence, first_seen (ISO-8601), last_seen (ISO-8601), incident_id. Set min_confidence (default 0.5) to narrow results. Reads only persisted incidents and never rotates, writes correlation state, or exposes secret values. Use a separately approved rotation tool to remediate.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Parameters for `phantom_leak_incidents_realtime`.",
        "properties": {
          "min_confidence": {
            "default": 0.5,
            "description": "Only return incidents whose confidence score is at or above this value\n(range 0.0–1.0). Defaults to 0.5 per spec (active incident threshold).",
            "format": "double",
            "type": "number"
          }
        },
        "title": "LeakIncidentsRealtimeParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_list_secrets",
      "description": "List all secret names in the Phantom vault. Returns names only — never exposes actual secret values. Use this to see what secrets are configured.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "phantom_list_with_expiry",
      "description": "List all secret names with their TTL/expiry status. Shows days remaining, EXPIRED flag, or 'no expiry' for each secret. Never returns secret values. Use it to audit which credentials need an explicit provider rotation or local expiry-policy review.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "show_expiry": {
            "default": true,
            "description": "Include TTL/expiry status for each secret (countdown, expired flag, etc.)",
            "type": "boolean"
          }
        },
        "title": "ListWithExpiryParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_remove_secret",
      "description": "Transactionally remove a secret's vault value, lifecycle configuration, and exact managed-dotenv mapping. DESTRUCTIVE — a successful removal is recoverable only from a separately retained backup. Uses exact before-images and rolls back only transaction-owned changes. Requires `confirm: true` plus an out-of-band `approval_token`; review the exact value-blind effect before approval.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. Defends against prompt-injected instructions\ndeleting secrets.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the secret to remove",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RemoveSecretParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotate",
      "description": "Regenerate all phantom tokens in .env. Old tokens become invalid — any running `phantom exec` / dev server that cached them will break until it picks up the new .env. Real secrets in the vault are unchanged. DESTRUCTIVE; requires `confirm: true`; the agent must ask the user for explicit consent before calling.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore invoking this tool. Rotating invalidates every live phantom token\nand will break any process that cached the old tokens (e.g. a running\n`phantom exec` or dev server) until it picks up the new .env.",
            "type": "boolean"
          }
        },
        "title": "RotateParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotate_promote",
      "description": "DEPRECATED hard denial: legacy candidates were local phm_cand_ placeholders, not provider-issued credentials. This tool never validates, promotes, or changes a vault value and ignores compatibility parameters. Live provider issuance is also hard-denied until compensated recovery exists; rotate at the provider and store the replacement interactively.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Deprecated compatibility parameters for the hard-denied promotion path.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Ignored compatibility field. No approval can enable this tool.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Ignored compatibility field. The tool is always denied.",
            "type": "boolean"
          },
          "name": {
            "description": "Ignored compatibility field. No candidate is promoted.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RotatePromoteParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotate_provider",
      "description": "Provider-rotation compatibility boundary. Shipped builds hard-deny automated live provider issuance before credential access or network I/O because the provider contract does not yet preserve a durable value-free recovery handle and verified abort path for local persistence failure. Unit-test mock providers are not production capability evidence. Current calls return a value-blind hard denial without consuming an approval or opening the vault. The confirm and approval fields are reserved for a future effectful, compensated implementation.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_rotate_provider`.\n\nCompatibility parameters for the shipped hard-denial boundary. Automated\nlive provider issuance is disabled before credential or network access until\na durable value-free recovery handle and verified abort path exist. No\nsecret value is accepted or returned.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.\nFormat: `\"<nonce_hex>:<approval_token_hex>\"`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be `true` — the calling agent must confirm with the\nuser before rotating a live credential. Vendor rotation calls\nexternal APIs and permanently invalidates the current key.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the secret to rotate (e.g. `STRIPE_SECRET_KEY`).",
            "type": "string"
          },
          "provider": {
            "default": null,
            "description": "Which provider to use: `\"stripe\"`, `\"github\"`, `\"aws\"`, `\"google\"`,\nor `\"vercel\"`. When set, it must match the `provider` field in the\nsecret's `[phantom.secrets.{name}.rotation_provider]` config block.\nOptional — when omitted, the provider is resolved from that config\nblock in `.phantom.toml`.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "required": [
          "name"
        ],
        "title": "RotateProviderParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotate_with_candidate",
      "description": "DEPRECATED hard denial: legacy shadow rotation generated only a local phm_cand_ placeholder, not a provider-issued credential. This tool never creates or stores a candidate and ignores compatibility parameters. Live provider issuance is also hard-denied until compensated recovery exists; rotate at the provider and store the replacement interactively.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Deprecated compatibility parameters for the hard-denied shadow path.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Ignored compatibility field. No approval can enable this tool.",
            "type": [
              "string",
              "null"
            ]
          },
          "auto_promote_ttl_secs": {
            "default": null,
            "description": "Ignored compatibility field. No candidate TTL is created.",
            "format": "uint64",
            "minimum": 0,
            "type": [
              "integer",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Ignored compatibility field. The tool is always denied.",
            "type": "boolean"
          },
          "name": {
            "description": "Ignored compatibility field. No candidate is created.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RotateWithCandidateParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotate_with_expiry",
      "description": "DEPRECATED compatibility name: atomically remap all local phm_ placeholders. The legacy days_ttl field is validated but no longer renews `rotated_at`, `expires_at`, or rotation_policy because no provider credential changes. Provider credentials and incident state remain unchanged. MUTATING — invalidates current Phantom placeholders; requires confirm:true plus an out-of-band approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore remapping all local Phantom tokens (invalidating cached\nplaceholders). Provider credentials and expiry metadata remain unchanged.",
            "type": "boolean"
          },
          "days_ttl": {
            "description": "Deprecated compatibility field. Must be greater than zero, but a local\nPhantom token remap does not renew credential TTL or lifecycle metadata.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "required": [
          "days_ttl"
        ],
        "title": "RotateWithExpiryParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_rotation_schedule_next",
      "description": "Return the next scheduled rotation time for a named secret. Reads the secret's effective RotationSchedule (per-secret override or global policy) and computes when rotation is next due. Returns: { name, strategy, next_rotation_unix (u64 or null), next_rotation_iso (ISO-8601 or null), last_rotated_unix (u64 or null), last_rotated_iso (ISO-8601 or null), overdue (bool) }. Read-only; no confirm required. Secret VALUES are never returned.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_rotation_schedule_next`.\n\nReturns the next scheduled rotation time (ISO-8601 + Unix epoch) for a\nnamed secret, based on its effective `RotationSchedule`.  Read-only; no\nconfirm required.",
        "properties": {
          "name": {
            "description": "Name of the secret to query (e.g. `OPENAI_API_KEY`).",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "RotationScheduleNextParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_secret_rotation_due",
      "description": "Query which secrets in the vault are due for rotation. Returns a JSON object with: due (list of secrets already expired), warning (list of secrets expiring within warn_days, default 7), ok (list of secrets with sufficient TTL remaining), no_ttl (list of secrets with no rotation policy set at all). Each entry has: name, status (expired|warning|ok|no_ttl), days_remaining (null if no TTL), expires_at (ISO-8601 or null). Secret VALUES are never returned. Read-only; no confirm required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "warn_days": {
            "default": 7,
            "description": "Warn when expiry is within this many days. Defaults to 7.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "RotationDueParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_secrets_auto_rotate",
      "description": "DEPRECATED compatibility name: remap the local phm_ placeholder for one already-protected secret. This does not rotate or validate the provider credential, does not change `rotated_at` / `expires_at` / TTL policy, does not clear leak incidents, and cannot sync an unchanged credential (`sync: true` is rejected). When audit logging is enabled, success records `secret.token_remapped`. MUTATING — atomically rewrites .env and requires `confirm: true` plus an out-of-band approval token. Secret VALUES are never returned or logged.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_secrets_auto_rotate`.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — the calling agent must confirm with the user\nbefore remapping the local Phantom token. This rewrites `.env` and can\ndisrupt running services that cached the old placeholder, but it does\nnot alter provider credentials or lifecycle metadata.",
            "type": "boolean"
          },
          "name": {
            "description": "Name of the already-protected secret whose local `phm_` placeholder\nshould be remapped. The provider credential is not rotated.",
            "type": "string"
          },
          "sync": {
            "default": false,
            "description": "Deprecated compatibility field. `true` is rejected because token\nremapping does not create a credential that can truthfully be synced.",
            "type": "boolean"
          }
        },
        "required": [
          "name"
        ],
        "title": "AutoRotateParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_secrets_expiry_check",
      "description": "Query the local vault for secrets that are expired or expiring soon. Returns a JSON array where each entry has: name (string), days_remaining (i64 — negative means already expired), expires_at (Unix epoch u64), status (human-readable string). Use the `days` parameter (default 7) to tune the look-ahead window. Secrets with no TTL set are omitted — use phantom_secret_rotation_due to also surface those. Secret VALUES are never returned. Read-only; no confirm required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_secrets_expiry_check`.",
        "properties": {
          "days": {
            "default": 7,
            "description": "Warn about secrets expiring within this many days (default: 7).\nSet to 0 to return only already-expired secrets.",
            "format": "uint64",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "ExpiryCheckParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_setup_workspace",
      "description": "Conversation-native, value-blind workspace setup. phase=propose returns an exact sealed plan; if the machine-local plan-seal key is absent, provisioning it requires confirm plus approval_token. phase=request_apply always requires both gates because it persists a bearerless trusted-terminal request. phase=status is read-only. MCP never claims or applies workspace changes.",
      "inputSchema": {
        "$defs": {
          "SetupWorkspacePhase": {
            "oneOf": [
              {
                "const": "propose",
                "description": "Inspect and return the current value-blind sealed plan.",
                "type": "string"
              },
              {
                "const": "request_apply",
                "description": "Create a bearerless request for trusted-terminal application.",
                "type": "string"
              },
              {
                "const": "status",
                "description": "Read authenticated, value-free request status.",
                "type": "string"
              }
            ]
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required for phases that persist machine-local state (`propose` when the\nplan-seal key has not been provisioned, and every `request_apply`).",
            "type": "boolean"
          },
          "phase": {
            "$ref": "#/$defs/SetupWorkspacePhase",
            "default": "propose",
            "description": "`propose` (default), `request_apply`, or `status`."
          },
          "plan_id": {
            "default": null,
            "description": "Exact plan digest returned by the latest propose phase.",
            "type": [
              "string",
              "null"
            ]
          },
          "pre_state_id": {
            "default": null,
            "description": "Exact keyed pre-state digest returned by the latest propose phase.",
            "type": [
              "string",
              "null"
            ]
          },
          "request_id": {
            "default": null,
            "description": "Bearerless request identifier returned by request_apply.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "title": "SetupWorkspaceParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_status",
      "description": "Read repository-local Phantom status without opening or provisioning a vault: project ID, managed dotenv protection counts, and configured service mappings. Vault backend and stored-secret inventory are deliberately not inspected; use phantom_list_secrets for the latter.",
      "inputSchema": {
        "properties": {},
        "type": "object"
      }
    },
    {
      "name": "phantom_sync",
      "description": "Show sync configuration and what secrets would be synced to deployment platforms (Vercel, Railway). This is an informational tool — actual sync requires platform API tokens. Use it to understand and explain the sync setup.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "platform": {
            "default": null,
            "description": "Platform to sync to (vercel, railway). If empty, syncs all configured targets.",
            "type": [
              "string",
              "null"
            ]
          },
          "project_id": {
            "default": null,
            "description": "Override project ID for this sync",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "title": "SyncParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_create",
      "description": "Request a new team from a separately commissioned hosted Pro pilot and entitled account; the public hosted service and self-serve Pro billing are not currently commissioned. If accepted, the authenticated user becomes the owner. Mutating provider request: requires confirm:true plus an out-of-band approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — confirms the user wants to request a new team\nfrom a separately commissioned hosted Pro pilot. This is not evidence\nof current public entitlement or self-serve billing.",
            "type": "boolean"
          },
          "name": {
            "description": "Name for the new team (human-readable label)",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "TeamCreateParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_invite",
      "description": "Invite someone to a team by GitHub username. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. The caller must be a team owner or admin; the hosted API permits assigning only member or admin (not owner). Mutating provider request: requires confirm:true plus an out-of-band approval_token.",
      "inputSchema": {
        "$defs": {
          "TeamInviteRole": {
            "enum": [
              "member",
              "admin"
            ],
            "type": "string"
          }
        },
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — confirms the user wants to add this person\nto the team. Defends against prompt-injected instructions silently\nexpanding team membership.",
            "type": "boolean"
          },
          "github_login": {
            "description": "GitHub username of the user to invite (no @ prefix)",
            "type": "string"
          },
          "role": {
            "$ref": "#/$defs/TeamInviteRole",
            "default": "member",
            "description": "Role to assign. Hosted invitations accept only `member` or `admin`;\nownership transfer is a separate lifecycle operation and is not exposed."
          },
          "team_id": {
            "description": "Team identifier (UUID)",
            "type": "string"
          }
        },
        "required": [
          "team_id",
          "github_login"
        ],
        "title": "TeamInviteParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_key_publish",
      "description": "One-time setup: register this device's public key with the team so you can send and receive encrypted vaults. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. Must be called before phantom_team_vault_push or phantom_team_vault_pull. Idempotent — safe to call again after a key rotation.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required for mutating operations that publish local key material.",
            "type": "boolean"
          },
          "team_id": {
            "description": "Team identifier (UUID)",
            "type": "string"
          }
        },
        "required": [
          "team_id"
        ],
        "title": "TeamIdParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_list",
      "description": "List teams the authenticated user belongs to. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. Returns team id, name, and role. Uses the stored cloud credential and makes a provider request; requires confirm plus approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Dual approval gate for tools whose only inputs are authority controls.",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true before the provider or persistent-state effect.",
            "type": "boolean"
          }
        },
        "title": "ApprovalParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_members",
      "description": "List members of a team by team_id. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. Returns GitHub login, email, and role. Uses the stored cloud credential and makes a provider request; requires confirm plus approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required for mutating operations that publish local key material.",
            "type": "boolean"
          },
          "team_id": {
            "description": "Team identifier (UUID)",
            "type": "string"
          }
        },
        "required": [
          "team_id"
        ],
        "title": "TeamIdParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_vault_pull",
      "description": "Download and decrypt the team vault for this project into the local vault. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. Use this (not phantom_cloud_pull) when secrets were shared by a teammate via phantom_team_vault_push. Overwrites local secrets: requires confirm:true.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — push/pull mutates the team's shared vault\n(push) or overwrites local secrets with the team copy (pull). Both\nare write operations that need user consent.",
            "type": "boolean"
          },
          "team_id": {
            "description": "Team identifier (UUID)",
            "type": "string"
          }
        },
        "required": [
          "team_id"
        ],
        "title": "TeamVaultParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_team_vault_push",
      "description": "Push this project's secrets to the shared team vault so all members can pull them. Requires a separately commissioned hosted service and entitled account; the public hosted service is not currently commissioned. Encrypts client-side for each member who has registered a key (phantom_team_key_publish). Mutating: requires confirm:true.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true — push/pull mutates the team's shared vault\n(push) or overwrites local secrets with the team copy (pull). Both\nare write operations that need user consent.",
            "type": "boolean"
          },
          "team_id": {
            "description": "Team identifier (UUID)",
            "type": "string"
          }
        },
        "required": [
          "team_id"
        ],
        "title": "TeamVaultParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_unwrap",
      "description": "Reverse phantom_wrap: restore original package.json scripts from their `:raw` variants and remove the `:raw` entries.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true because this modifies package.json.",
            "type": "boolean"
          }
        },
        "title": "UnwrapParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_validate_all",
      "description": "Run live credential health checks for all secrets in the vault. Each secret is validated against its target API (OpenAI, Stripe, GitHub, Anthropic, AWS, or a generic HTTP check). Returns a JSON compliance report with: total, valid, invalid, unreachable, not_checked counts and per-secret entries (name, validator, status, reason, checked_at). Secret VALUES are never returned or logged. This retrieves credentials, makes real outbound provider requests, and persists value-free validation metadata; it requires confirm plus approval_token and should run during a maintenance window.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required because validation retrieves credentials, calls provider APIs,\nand persists the value-free result metadata.",
            "type": "boolean"
          },
          "jobs": {
            "default": 4,
            "description": "Maximum number of concurrent validation jobs (default: 4, max: 16).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "ValidateAllParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_validate_secret",
      "description": "Query the last-known validation status for a specific secret. Returns: last_check_ts (Unix epoch, 0 = never checked), is_valid (bool), failure_reason (string or null), validator_name (which validator ran), and is_stale (whether the check is older than 24 h). This tool reads persisted ValidationMetadata — it does NOT make a live HTTP request. Use phantom_validate_all to trigger a fresh check. Read-only; no confirm required. Secret VALUES are never returned.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "name": {
            "description": "Name of the secret to query validation status for (e.g. OPENAI_API_KEY).\nNever the value — this tool only reads stored metadata.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "title": "ValidateSecretParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_validation_history",
      "description": "Retrieve the history of past automated validation runs. Each entry contains: started_at (Unix epoch), finished_at, total, pass, fail, unreachable, not_checked, and an optional error string. Use `limit` (default 20, max 100) to control how many recent entries are returned. Secret VALUES are never returned. Read-only; no confirm required.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Parameters for `phantom_validation_history`.",
        "properties": {
          "limit": {
            "default": 20,
            "description": "Maximum number of recent runs to return (default: 20, max: 100).",
            "format": "uint",
            "minimum": 0,
            "type": "integer"
          }
        },
        "title": "ValidationHistoryParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_validation_schedule",
      "description": "Get or set the automated background validation schedule. Pass `interval` to update (accepted values: 'hourly', '6h', '12h', 'daily', 'daily@2am', 'weekly', 'disabled'). Omit `interval` to read the current schedule and staleness status. Returns schedule, last_run_at, and staleness indicators. Read-only when interval is omitted. Providing interval persists scheduler state and requires confirm plus approval_token.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "additionalProperties": false,
        "description": "Parameters for `phantom_validation_schedule` (get/set schedule).",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required when `interval` is present because scheduler state is persisted.",
            "type": "boolean"
          },
          "interval": {
            "default": null,
            "description": "New schedule interval to set: \"hourly\", \"6h\", \"daily\", \"daily@2am\",\n\"weekly\", \"disabled\". Omit to read the current schedule only.",
            "type": [
              "string",
              "null"
            ]
          }
        },
        "title": "ValidationScheduleParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_why",
      "description": "Explain why an environment variable is or isn't protected by Phantom. Shows classification (Secret, PublicKey, NotSecret), whether it has a phantom token, and what heuristic matched.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "key": {
            "description": "Environment variable name to explain",
            "type": "string"
          }
        },
        "required": [
          "key"
        ],
        "title": "WhyParams",
        "type": "object"
      }
    },
    {
      "name": "phantom_wrap",
      "description": "Wrap package.json scripts with the installed local `phantom exec --` command so secrets are injected via the proxy at runtime. Saves originals as `script:raw` variants. Uses a heuristic to pick dev/start/build/serve/deploy scripts and skip lint/test/format scripts.",
      "inputSchema": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "properties": {
          "approval_token": {
            "default": null,
            "description": "Out-of-band approval token from `phantom mcp-approve <NONCE>`.",
            "type": [
              "string",
              "null"
            ]
          },
          "confirm": {
            "default": false,
            "description": "Required. Must be true because this modifies package.json.",
            "type": "boolean"
          },
          "only": {
            "default": [],
            "description": "Only wrap specific scripts (by name). If empty, uses default heuristic.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "skip": {
            "default": [],
            "description": "Skip specific scripts (by name)",
            "items": {
              "type": "string"
            },
            "type": "array"
          }
        },
        "title": "WrapParams",
        "type": "object"
      }
    }
  ],
  "packages": [
    {
      "registryType": "npm",
      "registryBaseUrl": "https://registry.npmjs.org",
      "identifier": "phantom-secrets-mcp",
      "version": "0.7.6",
      "transport": {
        "type": "stdio"
      }
    }
  ]
}
